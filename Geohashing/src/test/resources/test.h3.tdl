CREATE signals(@path='test_data.csv');
TRANSFORM signals textToColumnar(@delimiter=',') VALUE (_,_,_,lat,lon);

CALL h3 (@lat.column=lat,@lon.column=lon,@"hash.level"=5) INPUT signals OUTPUT with_hash;
