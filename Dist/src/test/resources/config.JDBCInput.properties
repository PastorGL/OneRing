dist.wrap=none

input.jdbc.driver=org.postgresql.Driver
input.jdbc.url=jdbc:postgresql://localhost/poi_test
input.jdbc.user=postgres
input.jdbc.password=

task.input=poi

task.operations=dummy

input.path.poi=jdbc:SELECT cat_id, name from categories where ? <= 0 and 0 <= ?

op.operation.dummy=nop

op.inputs.dummy=poi
op.outputs.dummy=p2
