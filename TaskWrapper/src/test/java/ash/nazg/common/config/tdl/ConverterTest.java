package ash.nazg.common.config.tdl;

import org.junit.Ignore;
import org.junit.Test;

import static org.hamcrest.CoreMatchers.is;
import static org.junit.Assert.assertThat;

public class ConverterTest {
@Ignore
    @Test
    public void testToJsonAndBack() throws Exception {
/*        TestRunner loader = new TestRunner();
        try {
            loader.before("/config.gridjoin.properties");
            TaskConfig underTestLoad = loader.getTaskConfig();

            Properties loaded = underTestLoad.getProperties();
            String json = PropertiesConverter.toJSON(null, underTestLoad.getProperties());
            Properties converted = PropertiesConverter.toProperties(json);

            assertThat(converted, is(loaded));

            System.out.println(json);
            System.out.println(converted);
        } finally {
            loader.after();
        }*/
    }
}
